@model ICollection<OwnerRestoreViewModel>
@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "All removed owners";
}

<style>
    .validation-message {
        color: red;
        display: none;
    }

    select:invalid + .validation-message {
        display: block;
    }
</style>

<div class="container d-flex justify-content-center">
    <div class="w-50">
        <h4 class="text-center">@ViewData["Title"]</h4>

        <form asp-area="Admin" asp-controller="Admin" asp-action="RestoreOwner" method="post">
            <div class="form-group">
                <div>
                    <label>Owner Name</label>
                    <select name="ownerId" class="form-control" required>
                        <option value="">Select an owner</option>
                        @foreach (var owner in Model)
                        {
                            <option value="@owner.Id">@owner.Name</option>
                        }
                    </select>
                    <span class="validation-message">Please select an owner to restore</span>
                </div>
            </div>

            <div class="form-group mt-2 justify-content-md-around text-center">
                <button type="submit" class="btn btn-outline-success mt-2">Select owner</button>
            </div>
        </form>

    </div>
</div>
