@model ICollection<VehicleRestoreViewModel>
@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "All deleted vehicles";
}

<style>
    .validation-message {
        color: red;
        display: none;
    }

    select:invalid + .validation-message {
        display: block;
    }
</style>

<div class="container d-flex justify-content-center">
    <div class="w-50">
        <h4 class="text-center">@ViewData["Title"]</h4>

        <form asp-area="Admin" asp-controller="Admin" asp-action="RestoreVehicle" method="post">
            <div class="form-group">
                <div>
                    <label>Vehicle Number</label>
                    <select name="regNumber" class="form-control" required>
                        <option value="">Select an owner</option>
                        @foreach (var vehicle in Model)
                        {
                            <option value="@vehicle.RegistrationNumber">@vehicle.RegistrationNumber</option>
                        }
                    </select>
                    <span class="validation-message">Please select a vehicle to restore</span>
                </div>
            </div>

            <div class="form-group mt-2 justify-content-md-around text-center">
                <button type="submit" class="btn btn-outline-success mt-2">Select vehicle</button>
            </div>
        </form>

    </div>
</div>
