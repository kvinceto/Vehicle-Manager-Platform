@model ICollection<CostCenterRestoreViewModel>
@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "All deleted cost centeres";
}

<style>
    .validation-message {
        color: red;
        display: none;
    }

    select:invalid + .validation-message {
        display: block;
    }
</style>

<div class="container d-flex justify-content-center">
    <div class="w-50">
        <h4 class="text-center">@ViewData["Title"]</h4>

        <form asp-area="Admin" asp-controller="Admin" asp-action="RestoreCostCenter" method="post">
            <div class="form-group">
                <div>
                    <label>Vehicle Number</label>
                    <select name="id" class="form-control" required>
                        <option value="">Select a cost center</option>
                        @foreach (var vehicle in Model)
                        {
                            <option value="@vehicle.Id">@vehicle.Name</option>
                        }
                    </select>
                    <span class="validation-message">Please select a cost center to restore</span>
                </div>
            </div>

            <div class="form-group mt-2 justify-content-md-around text-center">
                <button type="submit" class="btn btn-outline-success mt-2">Select cost center</button>
            </div>
        </form>

    </div>
</div>
